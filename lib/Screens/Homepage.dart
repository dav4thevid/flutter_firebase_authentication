import 'package:flutter/material.dart';

class HomepageScreen extends StatelessWidget {
  static String id = 'hpmepage';
  
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Homepage'),
    );
  }
}
